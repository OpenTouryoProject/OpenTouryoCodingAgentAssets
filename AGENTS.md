# プロジェクト ポリシー
アプリケーション・フレームワーク

https://github.com/OpenTouryoProject/OpenTouryo/

を使用したアプリケーション開発用エージェント・スキルを開発する開発エージェント用。

### Git 操作は行わない（状態を変える操作をしない）

**成果物の検収は人が行う。** エージェントは作業結果をワーキング ツリーに残すところまでを担当し、
Git 操作は人が手動で行う。

したがって、指示がない限り次を実行してはならない。

- `git add` / `commit` / `push`（検収前・未レビューの変更を確定・送信しない）
- `git checkout` / `switch` / `branch` / `reset` / `restore` / `stash`（人の作業状態や未保存の作業を壊す）

**参照系は制限しない。** 何を変更したかを正確に報告するために必要なため、次は自由に実行してよい。

- `git status` / `diff`（`--cached` 含む）/ `log` / `show` / `ls-files` / `check-ignore` / `blame`

作業が完了したら**何を変更したかを報告するに留める**。コミットの要否とタイミングは人が判断する。

<!--
  補足（執筆者向け）:
  インストラクションは「文脈」であって強制力を持たない。上記は遵守されやすい書き方に
  しているが、確実に阻止したい場合は仕組み側で塞ぐ必要がある。
    - Claude Code : PreToolUse フックで Bash(git commit:*) 等を deny する
    - 各プロダクト: 同等の機構があればそれを使う
  必要になったら install.ps1 に設定の配布を追加することを検討する。
-->

### 2つの AGENTS.md を混同しない

- **この repo 直下の `AGENTS.md`（本ファイル）** ＝ *このリポジトリ（エージェント・スキルそのもの）を開発する*ときのポリシー。
- **`src/instructions/AGENTS.md`** ＝ *配布物*。OpenTouryo を使うアプリ開発リポジトリへ配られる「概要」インストラクションの
  原本（利用側での挙動を規定）。**編集対象を取り違えない。** 配布物側は目安 200 行以内・常時コンテキスト前提で書く。

### 主張は実ソースで裏取りする（推測で書かない）

スキルの記述（クラス名・メソッド・分岐・パス・csproj の参照等）は、必ず**実物**で確認してから書く。

- C# 実ソース：**`C:\otr\OpenTouryo-03-20\root\programs\CS`**（実クローン。tag 03-20）。
- ドキュメント：**`D:\git\local\OpenTouryoProject\OpenTouryoDocuments\documents`**。
- 作者提示の as-built 成果物（例：`D:\git\local\OpenTouryoProject\OTRVCAS`）があれば `git diff`（閲覧のみ）で突き合わせる。
- **in-repo の `files/` ミラーは削除済み**（2026-07-20 以降は上記を参照）。裏が取れないことは書かず、確認するか作者に聞く。

### トークン規律（SKILL.md は目安 ~5000 以内）

- 各 `SKILL.md` の実効トークンを見積り、**~5000 を上限の目安**にする（`auth` の 4952 が実績上限の基準値）。
  見積りは「ASCII÷4 ＋ 非ASCII÷1.1、HTML コメント除去、ファイル全体」（scratchpad の `measure.py` 相当）。
- 超えたら**圧縮**する。詳細・edge case・as-built スクリプトは **`references/`・`samples/`・`examples.md`（on-demand）へ退避**
  ＝これらは SKILL 予算に含めない。SKILL 本文には毎回要る核心だけを残す。

### 意思決定は `DevelopmentHistory.md` に記録する

- 検証結果・設計判断は **§4.3**（実装から判明した事実・レポート反映）へ1行足す。**相対日付は絶対日付に変換**する。
- スキルを増減・改稿したら **§3 インベントリ**のトークン数・要約を更新する。

### スキルは prescriptive（利用側に判断させない）

- **スキル利用エージェントになるべく選択・判断させない。** 決め打ちできる所は固定規則にする。
- **variant 差は「csproj を見て判断」等、実測構成に根拠を置く**書き方にする（曖昧な指針で迷わせない）。
- **選択肢を提示するときは間引かない**（固定 N 択に畳まない・UI が絞るなら全候補を番号付きリスト）。

### 横断ルールは、モノによって「集約」せず「重複」させる（defense-in-depth）

配布物 `src/instructions/AGENTS.md` は**インストーラが再生成で上書き**し、利用側で誤編集・削除もされうる。

- したがって **スキル単体で誤動作すると実害が大きい横断ルールは、AGENTS.md に集約するだけでなく該当スキルにも重複記入**し、
  AGENTS.md が欠落しても効くようにする。
- ただし**全ルールを重複させるとトークンを食う**ので、対象は上記「実害が大きいもの」に絞る。
- 現時点の該当：**「選択肢は間引かない」**（`src/instructions/AGENTS.md` ＋ `opentouryo-project-setup-selection` ＋
  `opentouryo-base2-customize` に重複済み）。

### 作者の検証実行（field-test）レポートの反映手順

1. 各指摘を**実ソース／実 csproj／as-built に当てて裏取り**する（鵜呑みにしない）。
2. **既出・対応済みは正直に区別**して伝える。誤り・パスずれは是正する。
3. 反映先を配分（例：読む側は `-project-policy`、作る側は `-base2-customize`、ビルド機構は `-setup-build` の `examples.md`）。
4. トークンを測って上限内を確認し、`DevelopmentHistory.md` に記録する。

### スキルの標準準拠

- スキルの `name` は**ディレクトリ名と一致**、`description` は **1024 字以内**。
- HTML `<!-- -->` コメントは Claude Code では読み込み時に除去される（**執筆者メモ**の置き場に使える）。
- 破壊的・後戻りしにくい変更、新規ファイル作成などは**勝手に始めず確認**してから。
